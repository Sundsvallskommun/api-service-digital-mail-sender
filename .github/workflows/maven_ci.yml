name: "Call Java CI with Maven"

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - main
  schedule:
    # At 14:00 on every day (Please note: GitHub actions schedule is in UTC time).
    - cron: "0 11 * * *"

jobs:
  shared-workflows:
    uses: Sundsvallskommun/.github/.github/workflows/java-maven-ci.yml@main
